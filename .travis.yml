language: php

php:
    - 5.5
    - 5.6
    - 7.0
    - 7.1
    - 7.2
    - 7.3
    - 7.4
    - 8.0
    - 8.1
    - hhvm

sudo: false

matrix:
    fast_finish: true
    allow_failures:
        - php: hhvm

install:
    - composer install

script: vendor/bin/phpunit -c tests/phpunit.xml.dist --coverage-clover=coverage.clover
